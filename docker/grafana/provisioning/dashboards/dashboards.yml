# 6.2: load dashboard JSON from disk on startup (provisioned, not hand-imported).
apiVersion: 1

providers:
  - name: costpilot
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
